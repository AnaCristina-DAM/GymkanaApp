<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:theme="@style/AppTheme"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.anacristina.gymkana.MainActivity">

    <!-- Incluímos el layout de la ToolBar en el layout principal y le asignamos un ID que nos permita referenciarlo. -->
    <include
        layout="@layout/toolbar"
        android:id="@+id/toolbar_main">
    </include>

    <!-- Añadimos el fragmento correspondiente al mapa. -->
    <fragment
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.anacristina.gymkana.MapsActivity" />

</LinearLayout>